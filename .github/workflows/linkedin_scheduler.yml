name: LinkedIn Growth Bot

on:
  schedule:
    # Post at 07:00 UTC (10:00 Istanbul) and 14:00 UTC (17:00 Istanbul)
    # Peak engagement: morning scroll + end-of-workday
    - cron: '0 7,14 * * *'
  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run LinkedIn Workflow
      env:
        LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
        LINKEDIN_PERSON_URN: ${{ secrets.LINKEDIN_PERSON_URN }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        FORCED_VIBE: ${{ secrets.FORCED_VIBE }}
      run: |
        python linkedin_agents.py

    - name: Commit and Push Memory
      run: |
        git config --global user.name 'LinkedIn Bot'
        git config --global user.email 'bot@noreply.github.com'
        git add memory.json memory_archive.json 2>/dev/null || git add memory.json
        # Only commit if there are changes
        if git diff --staged --quiet; then
          echo "No changes to memory."
        else
          git commit -m "🧠 Update bot memory [skip ci]"
          git push
        fi
